RewriteEngine On
RewriteBase /

# Si el archivo o directorio existe, servirlo normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Si no existe, enviar todo a index.html
RewriteRule ^ index.html [L]
